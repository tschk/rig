# zig-host-rx4

Minimal Zig host demo for `rig add --rust rx4`.

```bash
cd examples/zig-host-rx4
rig init
rig add --rust rx4
zig build
```

Full ABI demo TBD once `rx4` ships a stable C ABI / cdylib façade.
